# -------------------------------------------
# 🌟 GiftIQ Project Configuration (.htaccess)
# -------------------------------------------

# 1️⃣ Enable URL Rewriting (optional for clean URLs)
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteBase /GiftIQ-main/
# Uncomment below if you use clean URLs:
# RewriteCond %{REQUEST_FILENAME} !-f
# RewriteCond %{REQUEST_FILENAME} !-d
# RewriteRule ^ index.php [L]
</IfModule>

# 2️⃣ Allow CORS (for FontAwesome, JS, and images)
<IfModule mod_headers.c>
    # Allow access from localhost
    Header set Access-Control-Allow-Origin "*"
    Header set Access-Control-Allow-Methods "GET, POST, OPTIONS"
    Header set Access-Control-Allow-Headers "Content-Type, Authorization"
    Header set Access-Control-Allow-Credentials "true"

    # Fix font loading MIME errors
    AddType font/woff2 .woff2
    AddType font/woff .woff
    AddType font/ttf .ttf
    AddType font/otf .otf
    AddType image/svg+xml .svg
</IfModule>

# 3️⃣ Security Headers (modern CSP + protection)
<IfModule mod_headers.c>
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-XSS-Protection "1; mode=block"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    Header always set Permissions-Policy "geolocation=(), microphone=()"
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
    
    # Use modern CSP rules (allow localhost assets)
    Header always set Content-Security-Policy "default-src 'self' http://localhost https://kit.fontawesome.com https://cdnjs.cloudflare.com; img-src * data: blob:; style-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com; script-src 'self' 'unsafe-inline' https://kit.fontawesome.com https://cdnjs.cloudflare.com; font-src 'self' data: https://cdnjs.cloudflare.com;"
</IfModule>

# 4️⃣ Cache control for performance
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType text/css "access plus 1 year"
    ExpiresByType text/javascript "access plus 1 year"
    ExpiresByType application/javascript "access plus 1 year"
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
    ExpiresByType font/woff2 "access plus 1 year"
    ExpiresByType font/woff "access plus 1 year"
    ExpiresByType font/ttf "access plus 1 year"
    ExpiresByType font/otf "access plus 1 year"
</IfModule>

# 5️⃣ Default Charset (avoid invalid charset header warning)
AddDefaultCharset UTF-8

# 6️⃣ Disable old headers (prevent duplicate warnings)
<IfModule mod_headers.c>
    Header unset X-Frame-Options
    Header unset X-Powered-By
    Header unset Pragma
    Header unset Expires
</IfModule>

# 7️⃣ Optional: Disable directory browsing
Options -Indexes
